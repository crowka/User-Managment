# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-goes-here

# Private API Keys (server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-goes-here

# Redis Configuration (if using)
UPSTASH_REDIS_REST_URL=your-redis-url
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Application Configuration
NODE_ENV=development
PORT=3000 # Optional: default port for development

# Testing Configuration
VITEST_ENVIRONMENT=jsdom
TEST_API_BASE_URL=http://localhost:3000 # Base URL for API testing

# Documentation for each variable:
# NEXT_PUBLIC_SUPABASE_URL: Your Supabase project URL (required)
# NEXT_PUBLIC_SUPABASE_ANON_KEY: Your Supabase anonymous key for client-side operations (required)
# SUPABASE_SERVICE_ROLE_KEY: Your Supabase service role key for admin operations (required for backend)
# UPSTASH_REDIS_REST_URL: Redis URL for caching (optional)
# UPSTASH_REDIS_REST_TOKEN: Redis authentication token (optional)
# NODE_ENV: Application environment (development/production)
# PORT: Port number for the development server
# VITEST_ENVIRONMENT: Test environment for Vitest
# TEST_API_BASE_URL: Base URL for API testing 